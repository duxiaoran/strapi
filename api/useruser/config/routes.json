{
  "routes": [
    {
      "method": "GET",
      "path": "/userusers/token",
      "handler": "Useruser.token",
      "config": {
        "policies": ["plugins.users-permissions.isAuthenticated"]
      }
    },
    {
      "method": "GET",
      "path": "/userusers",
      "handler": "Useruser.find",
      "config": {
        "policies": ["plugins.users-permissions.isAuthenticated"]
      }
    },
    {
      "method": "GET",
      "path": "/userusers/count",
      "handler": "Useruser.count",
      "config": {
        "policies": ["plugins.users-permissions.isAuthenticated"]
      }
    },
    {
      "method": "GET",
      "path": "/userusers/:id",
      "handler": "Useruser.findOne",
      "config": {
        "policies": ["plugins.users-permissions.isAuthenticated"]
      }
    },
    {
      "method": "POST",
      "path": "/userusers",
      "handler": "Useruser.create",
      "config": {
        "policies": ["plugins.users-permissions.isAuthenticated"]
      }
    },
    {
      "method": "PUT",
      "path": "/userusers/:id",
      "handler": "Useruser.update",
      "config": {
        "policies": ["plugins.users-permissions.isAuthenticated"]
      }
    },
    {
      "method": "DELETE",
      "path": "/userusers/:id",
      "handler": "Useruser.delete",
      "config": {
        "policies": ["plugins.users-permissions.isAuthenticated"]
      }
    }
  ]
}
